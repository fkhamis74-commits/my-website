{
  "name": "bicycle-store-site",
  "private": true,
  "version": "1.0.0",
  "description": "Static used-bicycle marketplace site (single index.html) with a local dev server for testing.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "cap:sync": "cp index.html www/index.html && npx cap sync"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  }
}
